events {}

http {
  server {
    listen 80;

    location / {
      proxy_pass http://frontend:3000;
    }

    # API (preferred)
    location /api/ {
      proxy_pass http://backend:5000/;
    }

    # 🔥 BACKWARD COMPAT FIX (THIS SOLVES YOUR ISSUE)
    location /todos {
      proxy_pass http://backend:5000/todos;
    }
  }
}

