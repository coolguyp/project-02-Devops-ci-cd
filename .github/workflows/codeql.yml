name: Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest   # use GitHub runner (faster than self-hosted usually)

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # 🔐 FAST secret scan (no Python install, no pip)
      - name: Secret scan with detect-secrets
        uses: reviewdog/action-detect-secrets@v2
        with:
          reporter: github-check
          fail-on-findings: true

      # Your build step (runs only if scan passes)
      - name: Continue build
        run: echo "✅ No secrets found. Build continues..."
